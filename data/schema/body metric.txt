//body metric
{
  _id: ObjectId,
  user_id: ObjectId,

  recorded_at: Date,

  measurements: {
    arms: {
      left_bicep_cm: Number,
      right_bicep_cm: Number,
      left_forearm_cm: Number,
      right_forearm_cm: Number,
      left_upper_arm_cm: Number,
      right_upper_arm_cm: Number
    },

    torso: {
      chest_relaxed_cm: Number,
      chest_flexed_cm: Number,
      waist_cm: Number,
      abdominal_cm: Number,
      hips_cm: Number
    },

    shoulders: {
      left_cm: Number,
      right_cm: Number,
      width_cm: Number
    },

    legs: {
      left_thigh_cm: Number,
      right_thigh_cm: Number,
      left_calf_cm: Number,
      right_calf_cm: Number
    },

    body: {
      height_cm: Number,
      weight_kg: Number,
      neck_cm: Number,
      wrist_cm: Number,
      body_fat_percent: Number
    }
  },

  notes: String
}

future addditions:
stamina,how long can i run at jogging/normal speed/sprinting
how far can i run at jogging/normal speed/sprinting
how long can i hold a plank
how long can i hold a handstand
how long can i hold a pullup
how many pushups can i do
how many situps can i do
how many squats can i do
how many pullups can i do
how long can i meditate
how long it takes to recover from exaustion
all m 
{
  _id: ObjectId,
  habitId: ObjectId,                    // ref → habitDefinitions

  periodDate: Date,                     // which day/week this log belongs to (normalized)
  loggedAt: Date,                       // exact timestamp of this entry

  // Completion — multiple entries per period are summed
  completionType: String,               // copied from definition
  targetValue: Number,                  // copied from definition
  loggedValue: Number,                  // what you logged THIS entry (e.g. 1hr)
  periodTotalValue: Number,             // running sum for the period (e.g. 3hr after 3 entries)
  completionPercent: Number,            // (periodTotalValue / targetValue) * 100

  // Time block tracking (only populated if timeBlock.enabled = true on definition)
  timeBlock: {
    planned: {
      start: String,                    // "05:00"
      end: String                       // "06:00"
    },
    actual: {
      start: Date,
      end: Date
    },
    minutesLate: Number                 // null if no time block set
  },

  // Mood & energy at log time
  mood: Number,                         // 0–10
  energy: Number,                       // 0–10
  notes: String,

  createdAt: Date
}